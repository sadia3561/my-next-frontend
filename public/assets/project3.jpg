import Image from "next/image";

const images = [
  { src: "/assests/project3.jpg", alt: "Project 3" },
];

export default function Gallery() {
  return (
    <div>
      {images.map((img) => (
        <Image
          key={img.src}
          src={img.src}
          alt={img.alt}
          width={600}
          height={400}
        />
      ))}
    </div>
  );
}
