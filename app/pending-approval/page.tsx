/*"use client";

import { useRouter } from "next/navigation";

export default function PendingApproval() {
  const router = useRouter();

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100 p-4">
      <div className="bg-white rounded-2xl shadow-lg p-8 max-w-md text-center">
        <h2 className="text-2xl font-bold text-blue-900 mb-4">⏳ Pending Approval</h2>
        <p className="mb-6 text-gray-700">
          Your organization account has been successfully registered. <br />
          It is now pending admin approval. You will receive an email once your account is approved.
        </p>

        <button
          onClick={() => router.push("/login")}
          className="bg-blue-700 text-white py-2 px-6 rounded-lg font-semibold hover:bg-blue-800 transition"
        >
          Back to Login
        </button>
      </div>
    </div>
  );
}*/
